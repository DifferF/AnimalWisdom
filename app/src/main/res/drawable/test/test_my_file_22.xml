//PACKAGE_BLOCK_01
package com.template.randomphrase;
//PACKAGE_BLOCK_01*/

//IMPORT_BLOCK_01
import android.os.Bundle;
import android.view.View;
import android.widget.Button;
import android.widget.TextView;
import androidx.appcompat.app.AppCompatActivity;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.Collections;
import java.util.List;
import java.util.Random;
import java.util.concurrent.ThreadLocalRandom;
import java.util.stream.Stream;
//IMPORT_BLOCK_01*/

public class ActivityOneTemplate extends AppCompatActivity {

        /*INSTANCE_BLOCK_01
        private TextView quoteTextViewTemplate;
        private Button quoteButtonTemplate;
        private String[] quotesTemplate;
        private Random randomTemplate = new Random();
        //INSTANCE_BLOCK_01*/

        /*INSTANCE_BLOCK_02
        protected TextView quoteTextViewTemplate;
        protected Button quoteButtonTemplate;
        protected String[] quotesTemplate;
        protected Random randomTemplate = new Random();
        //INSTANCE_BLOCK_02*/

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_one_layout_template);

        //TODO INITIALIZE_BLOCK
        /*INITIALIZE_BLOCK_01
        quotesTemplate = getResources().getStringArray(R.array.quotes_array_template);
        quoteTextViewTemplate = findViewById(R.id.quoteTextViewLayoutTemplate);
        quoteButtonTemplate = findViewById(R.id.quoteButtonLayoutTemplate);
        //INITIALIZE_BLOCK_01*/

        /*INITIALIZE_BLOCK_02
        initializationVariables();
        //INITIALIZE_BLOCK_02*/

        //TODO LISTENER_BLOCK

        /*LISTENER_BLOCK_01
        quoteButtonTemplate.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                int randomIndex = randomTemplate.nextInt(quotesTemplate.length);
                quoteTextViewTemplate.setText(quotesTemplate[randomIndex]);
            }
        });
        //LISTENER_BLOCK_01*/

         /*LISTENER_BLOCK_02
            quoteButtonTemplate.setOnClickListener(v -> {
            int randomIndex = randomTemplate.nextInt(quotesTemplate.length);
            quoteTextViewTemplate.setText(quotesTemplate[randomIndex]);
        });
        //LISTENER_BLOCK_02*/

        /*FUNone_BLOCK_01
        quoteTextViewTemplate.setText(quotesTemplate[randomTemplate.nextInt(quotesTemplate.length)]);
        //FUNone_BLOCK_01*/

        //TODO ???????? ????????? ?????? ??? ???????

        /*FUNone_BLOCK_02
        int randomIndexTemplate = ThreadLocalRandom.current().nextInt(quotesTemplate.length);
        quoteTextViewTemplate.setText(quotesTemplate[randomIndexTemplate]);
        //FUNone_BLOCK_02*/

        /*FUNone_BLOCK_03
        List<String> quotesListTemplate = new ArrayList<>(Arrays.asList(quotesTemplate));
        Collections.shuffle(quotesListTemplate);
        quoteTextViewTemplate.setText(quotesListTemplate.get(0));
        //FUNone_BLOCK_03*/

        /*FUNone_BLOCK_04
        int randomIndexTemplate= randomTemplate.nextInt(quotesTemplate.length);
        quoteTextViewTemplate.setText(quotesTemplate[randomIndexTemplate]);
        //FUNone_BLOCK_04*/

        /*FUNone_BLOCK_05
        List<String> quotesListTemplate = Arrays.asList(quotesTemplate);
        quoteTextViewTemplate.setText(quotesListTemplate.get(randomTemplate.nextInt(quotesListTemplate.size())));
        //FUNone_BLOCK_05*/

        /*FUNone_BLOCK_06
        quoteTextViewTemplate.setText(Stream.of(quotesTemplate)
                .skip(randomTemplate.nextInt(quotesTemplate.length))
                .findFirst()
                .orElse("No available"));
        //FUNone_BLOCK_06*/

    }

    //TODO INITIALIZE_BLOCK
    /*INITIALIZE_BLOCK_02
    void initializationVariables() {
        quotesTemplate = getResources().getStringArray(R.array.quotes_array_template);
        quoteTextViewTemplate = findViewById(R.id.quoteTextViewLayoutTemplate);
        quoteButtonTemplate = findViewById(R.id.quoteButtonLayoutTemplate);
    }
    //INITIALIZE_BLOCK_02*/

}
